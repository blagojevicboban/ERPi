<UserControl x:Class="ERPiApp.Views.Korisnici.KorisniciView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:core="clr-namespace:ERPiData.Models.Core;assembly=ERPiData"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Administracija korisnika i uloga (RBAC)" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                <TextBlock Text="Upravljanje korisničkim nalozima, ulogama i pravima pristupa sistemu." FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="BtnNoviKorisnik" Content="➕" Style="{StaticResource IconButtonStyle}" ToolTip="Novi korisnik" Click="BtnNoviKorisnik_Click" Margin="0,0,8,0"/>
                <Button x:Name="BtnOsvezi" Content="🔄" Style="{StaticResource IconButtonStyle}" ToolTip="Osveži spisak" Click="BtnOsvezi_Click"/>
            </StackPanel>
        </Grid>

        <!-- Pretraga -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="12,10" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TxtPretraga" Style="{StaticResource SearchTextBoxStyle}" Height="34" VerticalContentAlignment="Center" Padding="8,0"
                         ToolTip="Pretraga po korisničkom imenu ili imenu i prezimenu" TextChanged="TxtPretraga_TextChanged"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtUkupno" Text="Ukupno: 0 korisnika" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Spisak korisnika -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DataGrid x:Name="DgKorisnici" AutoGenerateColumns="False" IsReadOnly="True"
                      SelectionMode="Single" GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column" BorderThickness="0" Background="Transparent"
                      RowHeight="42" ColumnHeaderHeight="38">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding KorisnikId}" Width="60"/>
                    <DataGridTextColumn Header="Korisničko ime" Binding="{Binding KorisnickoIme}" Width="150"/>
                    <DataGridTextColumn Header="Ime i prezime" Binding="{Binding ImeIPrezime}" Width="2*"/>

                    <DataGridTemplateColumn Header="Uloga" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#DBEAFE"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Uloga}" Value="{x:Static core:UlogaKorisnika.Administrator}">
                                                    <Setter Property="Background" Value="#FCE7F3"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Uloga}" Value="{x:Static core:UlogaKorisnika.Gledalac}">
                                                    <Setter Property="Background" Value="#F3F4F6"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Uloga}" FontSize="12" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#1E40AF"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Uloga}" Value="{x:Static core:UlogaKorisnika.Administrator}">
                                                        <Setter Property="Foreground" Value="#9D174D"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Uloga}" Value="{x:Static core:UlogaKorisnika.Gledalac}">
                                                        <Setter Property="Foreground" Value="#4B5563"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#DCFCE7"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Background" Value="#FEE2E2"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Aktivan"/>
                                                <Setter Property="Foreground" Value="#15803D"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Text" Value="Deaktiviran"/>
                                                        <Setter Property="Foreground" Value="#B91C1C"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Poslednja prijava" Binding="{Binding PoslednjaPrijava, StringFormat=dd.MM.yyyy HH:mm}" Width="160"/>

                    <DataGridTemplateColumn Header="Akcije" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Button Content="✏️" Style="{StaticResource IconButtonStyle}" ToolTip="Izmeni korisnika" Click="BtnIzmeni_Click" Margin="0,0,4,0"/>
                                    <Button Content="🗑️" Style="{StaticResource IconButtonStyle}" ToolTip="Izbriši / deaktiviraj korisnika" Click="BtnBrisi_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
