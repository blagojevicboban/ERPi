<Window x:Class="ERPiApp.Views.Finansije.Partneri.ZatvoriStavkeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zatvori (upari) stavke" Height="620" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🔗 Zatvori (upari) stavke" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <DockPanel>
                    <TextBlock Text="Dugovna strana (Duguje)" FontWeight="Bold" DockPanel.Dock="Top" Margin="0,0,0,8"/>
                    <DataGrid x:Name="DgDuguje" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                              SelectionChanged="Selekcija_Changed">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                            <DataGridTextColumn Header="Opis" Binding="{Binding Opis}" Width="*"/>
                            <DataGridTextColumn Header="Preostalo" Binding="{Binding Preostalo, StringFormat=\{0:N2\}}" Width="90" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <DockPanel>
                    <TextBlock Text="Potražna strana (Potražuje)" FontWeight="Bold" DockPanel.Dock="Top" Margin="0,0,0,8"/>
                    <DataGrid x:Name="DgPotrazuje" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                              SelectionChanged="Selekcija_Changed">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                            <DataGridTextColumn Header="Opis" Binding="{Binding Opis}" Width="*"/>
                            <DataGridTextColumn Header="Preostalo" Binding="{Binding Preostalo, StringFormat=\{0:N2\}}" Width="90" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
            <TextBlock Text="Iznos zatvaranja (RSD):" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
            <TextBox x:Name="TxtIznos" Width="140" Height="34" VerticalContentAlignment="Center" Padding="8,0"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <TextBlock x:Name="TxtError" Foreground="#DC2626" VerticalAlignment="Center" TextWrapping="Wrap" MaxWidth="400" Margin="0,0,16,0" Visibility="Collapsed"/>
            <Button x:Name="BtnOtkazi" Content="Otkaži" Style="{StaticResource SecondaryButton}" Width="110" Margin="0,0,10,0" Click="BtnOtkazi_Click"/>
            <Button x:Name="BtnZatvori" Content="🔗 Zatvori" Style="{StaticResource ActionButtonStyle}" Width="130" Click="BtnZatvori_Click"/>
        </StackPanel>
    </Grid>
</Window>
