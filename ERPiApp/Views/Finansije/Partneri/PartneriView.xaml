<UserControl x:Class="ERPiApp.Views.Finansije.Partneri.PartneriView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1.2*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtPretraga" Width="220" Height="34" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretraga_TextChanged"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnNovPartner" Content="➕" Style="{StaticResource IconButtonStyle}" ToolTip="Nov partner" Click="BtnNovPartner_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnIzmeniPartnera" Content="✏️" Style="{StaticResource IconButtonStyle}" ToolTip="Izmeni izabranog partnera" Click="BtnIzmeniPartnera_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnObrisiPartnera" Content="🗑️" Style="{StaticResource IconButtonStyle}" ToolTip="Obriši izabranog partnera" Click="BtnObrisiPartnera_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnKamata" Content="💰" Style="{StaticResource IconButtonStyle}" ToolTip="Obračun zatezne kamate za izabranog partnera" Click="BtnKamata_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnIosIzvestaj" Content="📑" Style="{StaticResource IconButtonStyle}" ToolTip="IOS izveštaj — svi partneri" Click="BtnIosIzvestaj_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- LISTA PARTNERA -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="👥 Partneri" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                <DataGrid x:Name="DgPartneri" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                          SelectionChanged="DgPartneri_SelectionChanged" MouseDoubleClick="DgPartneri_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Šifra" Binding="{Binding SifraPartnera}" Width="100"/>
                        <DataGridTextColumn Header="Naziv" Binding="{Binding Naziv}" Width="*"/>
                        <DataGridTextColumn Header="PIB" Binding="{Binding Pib}" Width="110"/>
                        <DataGridTextColumn Header="Matični broj" Binding="{Binding MaticniBroj}" Width="110"/>
                        <DataGridTextColumn Header="Tip" Binding="{Binding TipPrikaz}" Width="220"/>
                        <DataGridTextColumn Header="Aktivan" Binding="{Binding IsActive}" Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="#E2E8F0"/>

        <!-- OTVORENE STAVKE IZABRANOG PARTNERA -->
        <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="16" Margin="0,16,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
                    <TextBlock x:Name="TxtStavkeNaslov" Text="📋 Otvorene stavke" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <CheckBox x:Name="ChkSveStavke" Content="Prikaži i zatvorene" VerticalAlignment="Center" Margin="0,0,16,0" Checked="ChkSveStavke_Changed" Unchecked="ChkSveStavke_Changed"/>
                        <Button x:Name="BtnZatvoriStavke" Content="🔗" Style="{StaticResource IconButtonStyle}" ToolTip="Zatvori (upari) dve stavke" Click="BtnZatvoriStavke_Click"/>
                    </StackPanel>
                </Grid>
                <DataGrid x:Name="DgStavke" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Konto" Binding="{Binding Konto}" Width="150"/>
                        <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                        <DataGridTextColumn Header="Br. naloga" Binding="{Binding BrojNaloga}" Width="80"/>
                        <DataGridTextColumn Header="Opis" Binding="{Binding Opis}" Width="*"/>
                        <DataGridTextColumn Header="Strana" Binding="{Binding Strana}" Width="80"/>
                        <DataGridTextColumn Header="Iznos (RSD)" Binding="{Binding OriginalniIznos, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        <DataGridTextColumn Header="Zatvoreno (RSD)" Binding="{Binding Zatvoreno, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        <DataGridTextColumn Header="Preostalo (RSD)" Binding="{Binding Preostalo, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="130"/>
                        <DataGridTextColumn Header="Dana kašnjenja" Binding="{Binding DanaKasnjenja}" Width="100" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
