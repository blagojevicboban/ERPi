<UserControl x:Class="ERPiApp.Views.Finansije.Partneri.PartneriView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,16,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
                    <TextBlock Text="👥 Partneri" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                    <Button x:Name="BtnNovPartner" Content="➕ Nov Partner" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" Padding="8,4" Click="BtnNovPartner_Click"/>
                </Grid>

                <TextBox x:Name="TxtPretragaPartnera" DockPanel.Dock="Top" Height="34" Margin="0,0,0,10" ToolTip="Pretraga partnera po nazivu, PIB-u ili matičnom broju" TextChanged="TxtPretragaPartnera_TextChanged"/>
                
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                    <RadioButton x:Name="RbPartneriSvi" Content="Svi" GroupName="FilterPartneri" IsChecked="True" Margin="0,0,14,0" Checked="RbFilterPartneri_Checked"/>
                    <RadioButton x:Name="RbPartneriKupci" Content="Kupci" GroupName="FilterPartneri" Margin="0,0,14,0" Checked="RbFilterPartneri_Checked"/>
                    <RadioButton x:Name="RbPartneriDobavljaci" Content="Dobavljači" GroupName="FilterPartneri" Checked="RbFilterPartneri_Checked"/>
                </StackPanel>

                <ListBox x:Name="LstPartneri" DockPanel.Dock="Top" BorderThickness="0" SelectionChanged="LstPartneri_SelectionChanged" PreviewMouseRightButtonDown="LstPartneri_PreviewMouseRightButtonDown">
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="✏️ Izmeni podatke/konto" Click="MiIzmeniPartnera_Click"/>
                            <MenuItem Header="🗑️ Obriši partnera" Click="MiObrisiPartnera_Click"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="4,6">
                                <TextBlock Text="{Binding Naziv}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding SifraPartnera}" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <DockPanel LastChildFill="True">
                    <WrapPanel DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="Saldo: " VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                        <TextBlock x:Name="TxtSaldoPartnera" Text="0,00" VerticalAlignment="Center" FontSize="16" FontWeight="Bold" Foreground="#7C3AED" Margin="0,0,16,0"/>
                        <Button x:Name="BtnKursnaLista" Content="💱" Style="{StaticResource PrimaryButtonStyle}" Background="#0284C7" Width="36" Padding="0" Margin="0,2,10,2" ToolTip="Prikaz kursne liste NBS" Click="BtnKursnaLista_Click"/>
                        <Button x:Name="BtnVerifikujRacun" Content="🔍" Style="{StaticResource PrimaryButtonStyle}" Background="#0D9488" Width="36" Padding="0" Margin="0,2,10,2" ToolTip="Provera tekućeg računa u NBS registru" Click="BtnVerifikujRacun_Click"/>
                        <Button x:Name="BtnStampajIOS" Content="🖨️" Style="{StaticResource PrimaryButtonStyle}" Background="#7C3AED" Width="36" Padding="0" Margin="0,2,10,2" ToolTip="Izvezi IOS (PDF)" Click="BtnStampajIOS_Click"/>
                        <Button x:Name="BtnObracunKamate" Content="💰" Style="{StaticResource PrimaryButtonStyle}" Background="#D97706" Width="36" Padding="0" Margin="0,2,10,2" IsEnabled="False" ToolTip="Obračun kamate je moguć samo za konto kupca (204/120)" Click="BtnObracunKamate_Click"/>
                        <Button x:Name="BtnExportExcelPartneri" Content="X" FontWeight="Bold" FontSize="20" Style="{StaticResource PrimaryButtonStyle}" Background="#059669" Width="36" Padding="0" Margin="0,2,0,2" ToolTip="Izvoz kartice partnera u Excel" Click="BtnExportExcelPartneri_Click"/>
                    </WrapPanel>
                    <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
                        <TextBlock x:Name="TxtNaslovPartnera" Text="Izaberite partnera sa leve strane" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                        <TextBlock x:Name="TxtPodnaslovPartnera" Text=" " FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <TabControl x:Name="TabStavke" Background="Transparent" BorderThickness="0" SelectionChanged="TabStavke_SelectionChanged">

                    <!-- ===================== ANALITIČKA KARTICA ===================== -->
                    <TabItem Header="📇 Analitička kartica">
                        <DockPanel Margin="0,12,0,0">
                            <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,12" VerticalAlignment="Center">
                                <TextBlock Text="Konto:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                <ComboBox x:Name="CmbKontoKartice" Width="260" DisplayMemberPath="Prikaz" SelectedValuePath="BrojKonta" SelectionChanged="CmbKontoKartice_SelectionChanged"/>
                                <TextBlock x:Name="TxtKontoJedini" VerticalAlignment="Center" Visibility="Collapsed"/>
                            </WrapPanel>
                            <DataGrid x:Name="DgOtvoreneStavke" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="110"/>
                                    <DataGridTextColumn Header="Br. naloga" Binding="{Binding BrojNaloga}" Width="120"/>
                                    <DataGridTextColumn Header="Opis" Binding="{Binding Opis}" Width="*"/>
                                    <DataGridTextColumn Header="Duguje (RSD)" Binding="{Binding Duguje, StringFormat=\{0:N2\}}" Width="140"/>
                                    <DataGridTextColumn Header="Potražuje (RSD)" Binding="{Binding Potrazuje, StringFormat=\{0:N2\}}" Width="140"/>
                                    <DataGridTextColumn Header="Saldo (RSD)" Binding="{Binding Saldo, StringFormat=\{0:N2\}}" Width="140"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </DockPanel>
                    </TabItem>

                    <!-- ===================== PRAVE OTVORENE STAVKE (IOS, zatvaranje) ===================== -->
                    <TabItem Header="🔗 Otvorene stavke (IOS)">
                        <DockPanel Margin="0,12,0,0">
                            <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,12">
                                <Button x:Name="BtnZatvoriStavke" Content="🔗 Upari / Zatvori stavke" Style="{StaticResource PrimaryButtonStyle}" Background="#7C3AED" Margin="0,0,8,0" Click="BtnZatvoriStavke_Click"/>
                                <Button x:Name="BtnIstorijaZatvaranja" Content="🕘 Istorija zatvaranja" Style="{StaticResource PrimaryButtonStyle}" Background="#64748B" Click="BtnIstorijaZatvaranja_Click"/>
                            </WrapPanel>
                            <DataGrid x:Name="DgPraveOtvoreneStavke" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" SelectionMode="Extended">
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding JeDospelo}" Value="True">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                                <Setter Property="Foreground" Value="#B91C1C"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                                    <DataGridTextColumn Header="Br. naloga" Binding="{Binding BrojNaloga}" Width="90"/>
                                    <DataGridTextColumn Header="Br. dokumenta" Binding="{Binding BrojDokumenta}" Width="110"/>
                                    <DataGridTextColumn Header="Opis" Binding="{Binding Opis}" Width="*"/>
                                    <DataGridTextColumn Header="Dospeva" Binding="{Binding ValutaDospela, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                                    <DataGridTextColumn Header="Strana" Binding="{Binding Strana}" Width="80"/>
                                    <DataGridTextColumn Header="Iznos (RSD)" Binding="{Binding OriginalniIznos, StringFormat=\{0:N2\}}" Width="110"/>
                                    <DataGridTextColumn Header="Zatvoreno (RSD)" Binding="{Binding Zatvoreno, StringFormat=\{0:N2\}}" Width="120"/>
                                    <DataGridTextColumn Header="Preostalo (RSD)" Binding="{Binding Preostalo, StringFormat=\{0:N2\}}" Width="120"/>
                                    <DataGridTextColumn Header="Dana kašnjenja" Binding="{Binding DanaKasnjenja}" Width="110"/>
                                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="130"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </DockPanel>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>
    </Grid>
</UserControl>
