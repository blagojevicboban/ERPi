<UserControl x:Class="ERPiApp.Views.Finansije.Bilansi.BilansUspehaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR & FILTERS -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <WrapPanel VerticalAlignment="Center">
                    <TextBlock Text="Od:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,4,0"/>
                    <DatePicker x:Name="DpOd" Height="34" Width="120" Margin="0,0,12,0"/>

                    <TextBlock Text="Do:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,4,0"/>
                    <DatePicker x:Name="DpDo" Height="34" Width="120" Margin="0,0,16,0"/>

                    <Button Content="🔄 Učitaj Bilans Uspeha" Style="{StaticResource ActionButtonStyle}" Height="34" Padding="14,0" Click="BtnUcitaj_Click"/>
                </WrapPanel>
            </DockPanel>
        </Border>

        <!-- DATA GRID -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel DockPanel.Dock="Top">
                <TextBlock Text="📈 Bilans Uspeha (Zvanične AOP Pozicije)" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                <DataGrid x:Name="DgBilansUspeha" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="AOP" Binding="{Binding AopCode}" Width="80" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Naziv Pozicije" Binding="{Binding Naziv}" Width="*"/>
                        <DataGridTextColumn Header="Konta" Binding="{Binding OpsegKonta}" Width="120"/>
                        <DataGridTextColumn Header="Iznos (RSD)" Binding="{Binding IznosTekucaGodina, StringFormat=\{0:N2\}}" Width="180" ElementStyle="{StaticResource NumericColumnElementStyle}" FontWeight="Bold"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
