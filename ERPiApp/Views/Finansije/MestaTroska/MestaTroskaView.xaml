<UserControl x:Class="ERPiApp.Views.Finansije.MestaTroska.MestaTroskaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <Grid Margin="16">
        <TabControl x:Name="TabMestaTroska" Background="Transparent" BorderThickness="0">

            <!-- ===================== TAB 1: ŠIFARNIK MESTA TROŠKA & PROJEKATA ===================== -->
            <TabItem Header="🎯 Šifarnik Mesta Troška &amp; Projekata">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- TOOLBAR -->
                    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel LastChildFill="True">
                            <WrapPanel DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,2,16,2">
                                <TextBlock Text="🔍 Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                                <TextBox x:Name="TxtPretraga" Height="34" Width="220" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretraga_TextChanged"/>
                            </WrapPanel>
                            <WrapPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Button x:Name="BtnNovoMesto" Content="➕ Novo mesto troška / projekat" Style="{StaticResource ActionButtonStyle}" Background="#4338CA" Margin="0,2,8,2" ToolTip="Kreiraj novo mesto troška ili projektni centar" Click="BtnNovoMesto_Click"/>
                                <Button x:Name="BtnIzmeni" Content="✏️ Izmeni" Style="{StaticResource ActionButtonStyle}" Background="#4B5563" Margin="0,2,8,2" ToolTip="Izmeni izabrano mesto troška" Click="BtnIzmeni_Click"/>
                                <Button x:Name="BtnObrisi" Content="🗑️ Obriši" Style="{StaticResource ActionButtonStyle}" Background="#EF4444" Margin="0,2,8,2" ToolTip="Obriši izabrano mesto troška" Click="BtnObrisi_Click"/>
                                <Button x:Name="BtnExportExcel" Content="X" FontWeight="Bold" FontSize="20" Style="{StaticResource ActionButtonStyle}" Background="#059669" Width="36" Padding="0" ToolTip="Izvoz šifarnika u Excel" Margin="0,2,8,2" Click="BtnExportExcel_Click"/>
                                <Button x:Name="BtnStampa" Content="🖨️" Style="{StaticResource ActionButtonStyle}" Background="#4F46E5" Width="36" Padding="0" ToolTip="Štampaj šifarnik mesta troška (PDF)" Margin="0,2,0,2" Click="BtnStampa_Click"/>
                            </WrapPanel>
                        </DockPanel>
                    </Border>

                    <!-- MASTER DATAGRID -->
                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgMestaTroska" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                                  GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Šifra" Binding="{Binding Sifra}" Width="120" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Naziv" Binding="{Binding Naziv}" Width="*"/>
                                <DataGridTextColumn Header="Tip jedinice" Binding="{Binding Tip}" Width="160" FontWeight="SemiBold"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding IsAktivno}" Width="100"/>
                                <DataGridTextColumn Header="Napomena" Binding="{Binding Napomena}" Width="200"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- ===================== TAB 2: ANALITIKA I PROFITABILNOST ===================== -->
            <TabItem Header="📊 Analitika &amp; Profitabilnost Projekata">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- TOOLBAR & FILTERS -->
                    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel LastChildFill="True">
                            <WrapPanel VerticalAlignment="Center">
                                <TextBlock Text="Mesto troška / Projekat:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                                <ComboBox x:Name="CmbIzvestajMesto" DisplayMemberPath="Naziv" SelectedValuePath="MestoTroskaId" Width="220" Height="34" SelectionChanged="CmbIzvestajFilter_SelectionChanged" Margin="0,0,16,0"/>

                                <TextBlock Text="Od:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                <DatePicker x:Name="DpOd" Height="34" SelectedDateChanged="DpFilter_SelectedDateChanged" Margin="0,0,12,0"/>

                                <TextBlock Text="Do:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                <DatePicker x:Name="DpDo" Height="34" SelectedDateChanged="DpFilter_SelectedDateChanged" Margin="0,0,16,0"/>

                                <Button Content="🔄 Osveži izveštaj" Style="{StaticResource ActionButtonStyle}" Height="34" Padding="14,0" Click="BtnOsveziAnalitiku_Click"/>
                            </WrapPanel>
                        </DockPanel>
                    </Border>

                    <!-- PROFITABILITY SUMMARY CARDS -->
                    <Grid Grid.Row="1" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Ukupni Prihodi (Konto 6xx) -->
                        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="14" Margin="0,0,6,0" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="UKUPNI PRIHODI PROJEKTA (Konto 6xx)" FontSize="10" FontWeight="SemiBold" Foreground="#059669"/>
                                <TextBlock x:Name="TxtUkupnoPrihodi" Text="0,00 RSD" FontSize="16" FontWeight="Bold" Foreground="#059669" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Ukupni Rashodi (Konto 5xx) -->
                        <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="14" Margin="6,0,6,0" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="UKUPNI RASHODI &amp; TROŠKOVI (Konto 5xx)" FontSize="10" FontWeight="SemiBold" Foreground="#DC2626"/>
                                <TextBlock x:Name="TxtUkupnoRashodi" Text="0,00 RSD" FontSize="16" FontWeight="Bold" Foreground="#DC2626" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Neto Profitabilnost -->
                        <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="14" Margin="6,0,0,0" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="NETO PROFITABILNOST (REZULTAT)" FontSize="10" FontWeight="SemiBold" Foreground="#4338CA"/>
                                <TextBlock x:Name="TxtNetoRezultat" Text="0,00 RSD" FontSize="16" FontWeight="Bold" Foreground="#4338CA" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- ANALYTICS DATAGRID -->
                    <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgAnalitika" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Broj konta" Binding="{Binding BrojKonta}" Width="120" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Naziv konta" Binding="{Binding NazivKonta}" Width="*"/>
                                <DataGridTextColumn Header="Duguje (Trošak)" Binding="{Binding UkupnoDuguje, StringFormat=\{0:N2\}}" Width="150"/>
                                <DataGridTextColumn Header="Potražuje (Prihod)" Binding="{Binding UkupnoPotrazuje, StringFormat=\{0:N2\}}" Width="150"/>
                                <DataGridTextColumn Header="Neto Saldo" Binding="{Binding Saldo, StringFormat=\{0:N2\}}" Width="160" FontWeight="Bold"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
