<UserControl x:Class="ERPiApp.Views.Finansije.PdvEvidencijaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Naslov -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="🧾 PDV Evidencija (KIR i KPR)" FontSize="22" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
            <TextBlock Text="Knjiga izdatih računa, Knjiga primljenih računa i zbirni obračun PDV obaveze za period" FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Filter bar -->
        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Period obračuna:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,12,0"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <DatePicker x:Name="DpOdDatuma" Width="130" Height="32"/>
                    <TextBlock Text=" do " VerticalAlignment="Center" Margin="8,0"/>
                    <DatePicker x:Name="DpDoDatuma" Width="130" Height="32"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="📄 Izvezi XML za ePorezi (PP-PDV)" Style="{StaticResource ActionButtonStyle}" Click="BtnIzveziPpPdvXml_Click" Background="#0284C7" Margin="0,0,8,0"/>
                    <Button Content="🔄 Osveži obračun" Style="{StaticResource ActionButtonStyle}" Click="BtnOsvezi_Click" Background="#2563EB"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tabovi -->
        <TabControl Grid.Row="2" Background="Transparent" BorderThickness="0">
            <!-- TAB 1: KIR -->
            <TabItem Header="📤 KIR — Knjiga Izdatih Računa (Izlaz)">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock x:Name="TxtKirUkupno" Text="Ukupno izlazni PDV: 0.00 RSD" FontWeight="Bold" FontSize="13" Foreground="#1E3A8A" VerticalAlignment="Center"/>
                        <Button DockPanel.Dock="Right" Content="📊 Izvoz Excel" Style="{StaticResource ActionButtonStyle}" Click="BtnExportExcelKir_Click" Background="#059669" HorizontalAlignment="Right"/>
                    </DockPanel>

                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgKir" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="R.br" Binding="{Binding RedniBroj}" Width="45"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding DatumRacuna, StringFormat=\{0:dd.MM.yyyy\}}" Width="85"/>
                                <DataGridTextColumn Header="Br. računa" Binding="{Binding BrojDokumenta}" Width="110"/>
                                <DataGridTextColumn Header="Kupac" Binding="{Binding PartnerNaziv}" Width="*"/>
                                <DataGridTextColumn Header="PIB" Binding="{Binding PartnerPib}" Width="90"/>
                                <DataGridTextColumn Header="Ukupno (RSD)" Binding="{Binding UkupnaNaknadaSaPdv, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="Osnovica 20%" Binding="{Binding Osnovica20, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="PDV 20%" Binding="{Binding Pdv20, StringFormat=\{0:N2\}}" Width="100" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="Osnovica 10%" Binding="{Binding Osnovica10, StringFormat=\{0:N2\}}" Width="100" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="PDV 10%" Binding="{Binding Pdv10, StringFormat=\{0:N2\}}" Width="90" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- TAB 2: KPR -->
            <TabItem Header="📥 KPR — Knjiga Primljenih Računa (Ulaz)">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock x:Name="TxtKprUkupno" Text="Ukupno prethodni PDV: 0.00 RSD" FontWeight="Bold" FontSize="13" Foreground="#1E3A8A" VerticalAlignment="Center"/>
                        <Button DockPanel.Dock="Right" Content="📊 Izvoz Excel" Style="{StaticResource ActionButtonStyle}" Click="BtnExportExcelKpr_Click" Background="#059669" HorizontalAlignment="Right"/>
                    </DockPanel>

                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgKpr" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="R.br" Binding="{Binding RedniBroj}" Width="45"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding DatumRacuna, StringFormat=\{0:dd.MM.yyyy\}}" Width="85"/>
                                <DataGridTextColumn Header="Br. računa" Binding="{Binding BrojDokumenta}" Width="110"/>
                                <DataGridTextColumn Header="Dobavljač" Binding="{Binding PartnerNaziv}" Width="*"/>
                                <DataGridTextColumn Header="PIB" Binding="{Binding PartnerPib}" Width="90"/>
                                <DataGridTextColumn Header="Ukupno (RSD)" Binding="{Binding UkupnaNaknadaSaPdv, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="Osnovica 20%" Binding="{Binding Osnovica20, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="Preth. PDV 20%" Binding="{Binding Pdv20, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="Osnovica 10%" Binding="{Binding Osnovica10, StringFormat=\{0:N2\}}" Width="100" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="Preth. PDV 10%" Binding="{Binding Pdv10, StringFormat=\{0:N2\}}" Width="100" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- TAB 3: POPDV OBRAČUN -->
            <TabItem Header="⚖️ POPDV Rekapitulacija Obaveze">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
                    <StackPanel MaxWidth="700" HorizontalAlignment="Left">
                        <TextBlock Text="Rekapitulacija PDV obaveze za izabrani period" FontSize="16" FontWeight="Bold" Foreground="#1E3A8A" Margin="0,0,0,15"/>

                        <Border Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="1. Izlazni PDV (KIR - Obaveza po računu 20% + 10%):" FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtObracunKirPdv" Text="0.00 RSD" FontSize="14" FontWeight="Bold" Foreground="#2563EB"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="2. Prethodni PDV (KPR - Odbitni pretporez 20% + 10%):" FontSize="14" FontWeight="SemiBold" Margin="0,12,0,0"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtObracunKprPdv" Text="0.00 RSD" FontSize="14" FontWeight="Bold" Foreground="#D97706" Margin="0,12,0,0"/>

                                <Separator Grid.Row="2" Grid.ColumnSpan="2" Margin="0,16"/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="KONAČNI OBRAČUN PDV-A:" FontSize="15" FontWeight="Bold"/>
                                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="TxtObracunKonačni" Text="0.00 RSD" FontSize="16" FontWeight="Bold" Foreground="#059669"/>
                            </Grid>
                        </Border>

                        <Border x:Name="PnlStatusPdv" Background="#DCFCE7" BorderBrush="#86EFAC" BorderThickness="1" CornerRadius="8" Padding="16">
                            <TextBlock x:Name="TxtStatusPdvPoruka" Text="Obračun je urađen." FontSize="13" FontWeight="SemiBold" Foreground="#166534"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
