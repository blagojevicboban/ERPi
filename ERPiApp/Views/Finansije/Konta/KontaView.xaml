<UserControl x:Class="ERPiApp.Views.Finansije.Konta.KontaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <WrapPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2,16,2">
                    <TextBlock Text="Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtPretraga" Style="{StaticResource SearchTextBoxStyle}" Width="220" Height="34" TextChanged="TxtPretraga_TextChanged"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2,0,2">
                    <Button x:Name="BtnNoviKonto" Content="➕ Novi konto" Style="{StaticResource ActionButtonStyle}" Margin="0,0,8,0" Click="BtnNoviKonto_Click"/>
                    <Button x:Name="BtnIzmeniKonto" Content="✏️ Izmeni" Style="{StaticResource ActionButtonStyle}" Background="#D97706" Margin="0,0,8,0" Click="BtnIzmeniKonto_Click" IsEnabled="False"/>
                    <Button x:Name="BtnObrisiKonto" Content="🗑️ Obriši" Style="{StaticResource ActionButtonStyle}" Background="#DC2626" Margin="0,0,8,0" Click="BtnObrisiKonto_Click" IsEnabled="False"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <!-- Main Data Grid -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="📋 Kontni plan (Šifarnik konta)" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                <DataGrid x:Name="DgKonta" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" MouseDoubleClick="DgKonta_MouseDoubleClick" SelectionChanged="DgKonta_SelectionChanged">
                    <DataGrid.ContextMenu>
                        <ContextMenu x:Name="CmKonta">
                            <MenuItem Header="➕ Novi konto" Click="BtnNoviKonto_Click"/>
                            <MenuItem x:Name="CmiIzmeniKonto" Header="✏️ Izmeni" Click="BtnIzmeniKonto_Click"/>
                            <MenuItem x:Name="CmiObrisiKonto" Header="🗑️ Obriši" Click="BtnObrisiKonto_Click"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <EventSetter Event="PreviewMouseRightButtonDown" Handler="DataGridRow_PreviewMouseRightButtonDown"/>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Broj konta" Binding="{Binding BrojKonta}" Width="140"/>
                        <DataGridTextColumn Header="Naziv konta" Binding="{Binding NazivKonta}" Width="240"/>
                        <DataGridTextColumn Header="Stari konto" Binding="{Binding StariKonto}" Width="90"/>
                        <DataGridTextColumn Header="Ulica" Binding="{Binding Ulica}" Width="150"/>
                        <DataGridTextColumn Header="Mesto" Binding="{Binding Mesto}" Width="120"/>
                        <DataGridTextColumn Header="Žiro račun" Binding="{Binding ZiroRacun}" Width="140"/>
                        <DataGridTextColumn Header="Telefon" Binding="{Binding Telefon}" Width="110"/>
                        <DataGridTextColumn Header="Klasa" Binding="{Binding Klasa, StringFormat='Klasa \{0\}'}" Width="120"/>
                        <DataGridCheckBoxColumn Header="Sintetika" Binding="{Binding IsSintetika}" Width="100"/>
                        <DataGridTextColumn Header="Vrsta" Binding="{Binding VrstaKonta}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
