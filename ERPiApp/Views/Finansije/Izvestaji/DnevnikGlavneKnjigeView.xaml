<UserControl x:Class="ERPiApp.Views.Finansije.Izvestaji.DnevnikGlavneKnjigeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR & FILTERS -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnUcitaj" Content="🔄" Style="{StaticResource IconButtonStyle}" ToolTip="Osveži" Margin="0,0,8,0" Click="BtnUcitaj_Click"/>
                    <Button x:Name="BtnStampajPdf" Content="🖨️" Style="{StaticResource IconButtonStyle}" ToolTip="Štampaj PDF" Margin="0,0,8,0" Click="BtnStampajPdf_Click"/>
                    <Button x:Name="BtnExportExcel" Content="📊" Style="{StaticResource IconButtonStyle}" ToolTip="Izvoz u Excel" Click="BtnExportExcel_Click"/>
                </StackPanel>
                <WrapPanel VerticalAlignment="Center">
                    <TextBlock Text="Od:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,4,0"/>
                    <DatePicker x:Name="DpOd" Height="34" Width="120" Margin="0,0,12,0"/>

                    <TextBlock Text="Do:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,4,0"/>
                    <DatePicker x:Name="DpDo" Height="34" Width="120" Margin="0,0,12,0"/>
                </WrapPanel>
            </DockPanel>
        </Border>

        <!-- DATA GRID -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="DgDnevnik" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nalog" Binding="{Binding BrojNaloga}" Width="70"/>
                        <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                        <DataGridTextColumn Header="Dokument / Opis" Binding="{Binding DokumentOpis}" Width="*"/>
                        <DataGridTextColumn Header="Konto" Binding="{Binding BrojKonta}" Width="80"/>
                        <DataGridTextColumn Header="Naziv konta" Binding="{Binding NazivKonta}" Width="220"/>
                        <DataGridTextColumn Header="Duguje" Binding="{Binding Duguje, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        <DataGridTextColumn Header="Potražuje" Binding="{Binding Potrazuje, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Border Grid.Row="1" Background="#EFF6FF" CornerRadius="6" Padding="12,10" Margin="0,8,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="Ukupno duguje: " FontWeight="SemiBold"/>
                        <TextBlock x:Name="TxtUkupnoDuguje" Text="0,00" FontWeight="Bold" Margin="0,0,20,0"/>
                        <TextBlock Text="Ukupno potražuje: " FontWeight="SemiBold"/>
                        <TextBlock x:Name="TxtUkupnoPotrazuje" Text="0,00" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
