<UserControl x:Class="ERPiApp.Views.Finansije.Izvestaji.BrutoBilansView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR & FILTERS -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <WrapPanel VerticalAlignment="Center">
                    <TextBlock Text="Od:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,4,0"/>
                    <DatePicker x:Name="DpOd" Height="34" Width="120" Margin="0,0,12,0"/>

                    <TextBlock Text="Do:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,4,0"/>
                    <DatePicker x:Name="DpDo" Height="34" Width="120" Margin="0,0,16,0"/>

                    <TextBlock Text="Klasa:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,4,0"/>
                    <ComboBox x:Name="CmbKlasa" Height="34" Width="70" SelectedIndex="0" Margin="0,0,16,0">
                        <ComboBoxItem Content="Sve"/>
                        <ComboBoxItem Content="0"/>
                        <ComboBoxItem Content="1"/>
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="5"/>
                        <ComboBoxItem Content="6"/>
                        <ComboBoxItem Content="7"/>
                        <ComboBoxItem Content="8"/>
                        <ComboBoxItem Content="9"/>
                    </ComboBox>

                    <CheckBox x:Name="ChkSaTotalima" Content="Totali po sintetičkim kontima" VerticalAlignment="Center" IsChecked="True" Margin="0,0,16,0"/>

                    <Button Content="🔄 Učitaj" Style="{StaticResource ActionButtonStyle}" Height="34" Padding="14,0" Margin="0,0,8,0" Click="BtnUcitaj_Click"/>
                    <Button x:Name="BtnStampajPdf" Content="🖨️ PDF" Style="{StaticResource ActionButtonStyle}" Background="#4F46E5" Height="34" Padding="14,0" Margin="0,0,8,0" Click="BtnStampajPdf_Click"/>
                    <Button x:Name="BtnExportExcel" Content="X" FontWeight="Bold" FontSize="20" Style="{StaticResource ActionButtonStyle}" Background="#059669" Height="34" Width="36" Padding="0" ToolTip="Izvoz bruto bilansa u Excel" Click="BtnExportExcel_Click"/>
                </WrapPanel>
            </DockPanel>
        </Border>

        <!-- DATA GRID -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="DgBrutoBilans" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Broj konta" Binding="{Binding BrojKonta}" Width="120" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Naziv konta" Binding="{Binding NazivKonta}" Width="*"/>
                        <DataGridTextColumn Header="Duguje (Promet)" Binding="{Binding Duguje, StringFormat=\{0:N2\}}" Width="150"/>
                        <DataGridTextColumn Header="Potražuje (Promet)" Binding="{Binding Potrazuje, StringFormat=\{0:N2\}}" Width="150"/>
                        <DataGridTextColumn Header="Saldo Duguje" Binding="{Binding SaldoDuguje, StringFormat=\{0:N2\}}" Width="150" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="Saldo Potražuje" Binding="{Binding SaldoPotrazuje, StringFormat=\{0:N2\}}" Width="150" FontWeight="SemiBold"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Border Grid.Row="1" Background="#EFF6FF" CornerRadius="6" Padding="12,10" Margin="0,8,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="1" Text="UKUPNO BRUTO BILANS:" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TxtUkupnoDuguje" Grid.Column="2" Text="0,00" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TxtUkupnoPotrazuje" Grid.Column="3" Text="0,00" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TxtUkupnoSaldoDuguje" Grid.Column="4" Text="0,00" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#059669" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TxtUkupnoSaldoPotrazuje" Grid.Column="5" Text="0,00" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#DC2626"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
