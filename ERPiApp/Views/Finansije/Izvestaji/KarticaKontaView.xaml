<UserControl x:Class="ERPiApp.Views.Finansije.Izvestaji.KarticaKontaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,16,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="📋 Kontni plan" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                <TextBox x:Name="TxtPretragaKonta" Style="{StaticResource SearchTextBoxStyle}" DockPanel.Dock="Top" Height="34" Margin="0,0,0,8" ToolTip="Pretraga po broju ili nazivu konta" TextChanged="TxtPretragaKonta_TextChanged"/>
                <CheckBox x:Name="ChkSamoSaPrometom" Content="Samo konta sa knjiženjima" DockPanel.Dock="Top" Margin="0,0,0,12" IsChecked="True" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12" ToolTip="Označi za štampu sve konta u opsegu, ili samo &quot;Od&quot; za celu klasu (npr. 5)">
                    <TextBox x:Name="TxtOpsegOd" Width="60" Height="28" VerticalContentAlignment="Center" Padding="4,0" ToolTip="Od konta (ili klasa, npr. 5)"/>
                    <TextBlock Text="–" VerticalAlignment="Center" Margin="4,0"/>
                    <TextBox x:Name="TxtOpsegDo" Width="60" Height="28" VerticalContentAlignment="Center" Padding="4,0" ToolTip="Do konta (opciono)"/>
                    <Button x:Name="BtnOznaciOpseg" Content="Označi opseg" Style="{StaticResource ActionButtonStyle}" Height="28" Padding="10,0" Margin="8,0,0,0" ToolTip="Označi sva konta u navedenom opsegu kontnog plana" Click="BtnOznaciOpseg_Click"/>
                </StackPanel>
                <DataGrid x:Name="LstKonta" AutoGenerateColumns="False" SelectionMode="Single" SelectionUnit="FullRow" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" ToolTip="Desni klik za dodatne opcije" SelectionChanged="LstKonta_SelectionChanged" PreviewMouseRightButtonDown="LstKonta_PreviewMouseRightButtonDown">
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="🖨️ Štampaj karticu" Click="CtxStampajKarticu_Click"/>
                            <MenuItem Header="📊 Izvoz u Excel" Click="CtxExportExcelKartica_Click"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="30">
                            <DataGridCheckBoxColumn.Header>
                                <CheckBox x:Name="ChkSveKonta" IsThreeState="True" HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Checked="ChkSveKonta_Checked" Unchecked="ChkSveKonta_Unchecked"/>
                            </DataGridCheckBoxColumn.Header>
                            <DataGridCheckBoxColumn.ElementStyle>
                                <Style TargetType="CheckBox">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridCheckBoxColumn.ElementStyle>
                        </DataGridCheckBoxColumn>
                        <DataGridTextColumn Header="Konto" Binding="{Binding BrojKonta}" Width="70" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Naziv" Binding="{Binding NazivKonta}" Width="*" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <DockPanel LastChildFill="True">
                    <WrapPanel DockPanel.Dock="Right" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2,12,2">
                            <TextBlock Text="Od:" VerticalAlignment="Center" Margin="0,0,4,0" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                            <DatePicker x:Name="DpKarticaOd" Width="115" Height="32" Margin="0,0,8,0" ToolTip="Početak perioda za karticu konta" SelectedDateChanged="Period_Changed"/>
                            <TextBlock Text="Do:" VerticalAlignment="Center" Margin="0,0,4,0" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                            <DatePicker x:Name="DpKarticaDo" Width="115" Height="32" ToolTip="Kraj perioda za karticu konta" SelectedDateChanged="Period_Changed"/>
                        </StackPanel>
                        <Button x:Name="BtnStampajKartice" Content="🖨️" Style="{StaticResource ActionButtonStyle}" Width="36" Padding="0" ToolTip="Štampaj karticu/e (PDF)" VerticalAlignment="Center" Margin="0,2,8,2" Click="BtnStampajKartice_Click" IsEnabled="False"/>
                        <Button x:Name="BtnExportExcelKartica" Content="X" FontWeight="Bold" FontSize="20" Style="{StaticResource ActionButtonStyle}" Background="#059669" Width="36" Padding="0" ToolTip="Izvoz kartice konta u Excel" VerticalAlignment="Center" Margin="0,2,0,2" Click="BtnExportExcelKartica_Click"/>
                    </WrapPanel>

                    <StackPanel VerticalAlignment="Center" Margin="0,0,16,0">
                        <TextBlock x:Name="TxtNaslovKonta" Text="Izaberite konto sa leve strane" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="TxtPodnaslovKonta" Text=" " FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="DgKartica" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" ToolTip="Dupli klik na stavku otvara nalog" MouseDoubleClick="DgKartica_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="110"/>
                            <DataGridTextColumn Header="Br. naloga" Binding="{Binding BrojNaloga}" Width="120"/>
                            <DataGridTextColumn Header="Opis" Binding="{Binding Opis}" Width="*"/>
                            <DataGridTextColumn Header="Promena" Binding="{Binding OpisPromene}" Width="150"/>
                            <DataGridTextColumn Header="Duguje (RSD)" Binding="{Binding Duguje, StringFormat=\{0:N2\}}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            <DataGridTextColumn Header="Potražuje (RSD)" Binding="{Binding Potrazuje, StringFormat=\{0:N2\}}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            <DataGridTextColumn Header="Saldo (RSD)" Binding="{Binding Saldo, StringFormat=\{0:N2\}}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="1" Background="#EFF6FF" CornerRadius="6" Padding="12,10" Margin="0,8,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="140"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="3" Text="UKUPNO:" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock x:Name="TxtSumaDuguje" Grid.Column="4" Text="0,00" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock x:Name="TxtSumaPotrazuje" Grid.Column="5" Text="0,00" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock x:Name="TxtSumaSaldo" Grid.Column="6" Text="0,00" FontWeight="Bold" FontSize="15" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#2563EB"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
