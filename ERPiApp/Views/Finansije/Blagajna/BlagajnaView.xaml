<UserControl x:Class="ERPiApp.Views.Finansije.Blagajna.BlagajnaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <Grid Margin="16">
        <TabControl x:Name="TabBlagajna" Background="Transparent" BorderThickness="0">

            <!-- TAB 1: NALOZI BLAGAJNE -->
            <TabItem Header="💰 Dinarska &amp; Devizna Blagajna">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel LastChildFill="True">
                            <WrapPanel DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Button x:Name="BtnNovaUplata" Content="📥 Nalog za uplatu" Style="{StaticResource ActionButtonStyle}" Background="#059669" Margin="0,2,8,2" ToolTip="Kreiraj novi nalog za uplatu u blagajnu" Click="BtnNovaUplata_Click"/>
                                <Button x:Name="BtnNovaIsplata" Content="📤 Nalog za isplatu" Style="{StaticResource ActionButtonStyle}" Background="#D97706" Margin="0,2,8,2" ToolTip="Kreiraj novi nalog za isplatu iz blagajne" Click="BtnNovaIsplata_Click"/>
                                <Button x:Name="BtnIzmeni" Content="✏️ Izmeni" Style="{StaticResource ActionButtonStyle}" Background="#4B5563" Margin="0,2,8,2" ToolTip="Izmeni izabrani blagajnički nalog" Click="BtnIzmeni_Click"/>
                                <Button x:Name="BtnObrisi" Content="🗑️ Obriši" Style="{StaticResource ActionButtonStyle}" Background="#EF4444" Margin="0,2,8,2" ToolTip="Obriši izabrani blagajnički nalog" Click="BtnObrisi_Click"/>
                                <Button x:Name="BtnKnjizi" Content="✅ Proknjiži u GK" Style="{StaticResource ActionButtonStyle}" Background="#10B981" Margin="0,2,8,2" ToolTip="Proknjiži izabrane naloge u Glavnu knjigu" Click="BtnKnjizi_Click"/>
                            </WrapPanel>
                            <WrapPanel VerticalAlignment="Center">
                                <StackPanel Orientation="Vertical" Margin="0,0,16,0" VerticalAlignment="Center">
                                    <TextBlock Text="Blagajna:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                    <ComboBox x:Name="CmbFilterBlagajna" Width="150" Height="34" SelectionChanged="CmbFilterBlagajna_SelectionChanged">
                                        <ComboBoxItem Content="Sve blagajne" IsSelected="True"/>
                                        <ComboBoxItem Content="Dinarska (2430)"/>
                                        <ComboBoxItem Content="Devizna (2440)"/>
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                                    <TextBlock Text="Pretraga:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                    <TextBox x:Name="TxtPretraga" Style="{StaticResource SearchTextBoxStyle}" Height="34" Width="200" TextChanged="TxtPretraga_TextChanged"/>
                                </StackPanel>
                            </WrapPanel>
                        </DockPanel>
                    </Border>

                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgBlagajnickiNalozi" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                                  GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Broj naloga" Binding="{Binding BrojNaloga}" Width="140" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Blagajna" Binding="{Binding VrstaBlagajne}" Width="100"/>
                                <DataGridTextColumn Header="Vrsta" Binding="{Binding VrstaNaloga}" Width="90" FontWeight="SemiBold"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                                <DataGridTextColumn Header="Uplatilac / Isplatilac" Binding="{Binding UplatilacIsplatilac}" Width="180"/>
                                <DataGridTextColumn Header="Svrha uplate / isplate" Binding="{Binding Svrha}" Width="*"/>
                                <DataGridTextColumn Header="Konto" Binding="{Binding BrojKontaProtu}" Width="80"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" FontWeight="SemiBold"/>
                                <DataGridTextColumn Header="Iznos" Binding="{Binding Iznos, StringFormat=\{0:N2\}}" Width="140" FontWeight="Bold"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- TAB 2: BLAGAJNIČKI DNEVNIK -->
            <TabItem Header="📖 Blagajnički Dnevnik">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel LastChildFill="True">
                            <WrapPanel VerticalAlignment="Center">
                                <StackPanel Orientation="Vertical" Margin="0,0,16,0" VerticalAlignment="Center">
                                    <TextBlock Text="Blagajna:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                    <ComboBox x:Name="CmbDnevnikBlagajna" Width="160" Height="34" SelectionChanged="CmbDnevnikFilter_SelectionChanged">
                                        <ComboBoxItem Content="Dinarska (2430)" IsSelected="True"/>
                                        <ComboBoxItem Content="Devizna (2440)"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel Orientation="Vertical" Margin="0,0,12,0" VerticalAlignment="Center">
                                    <TextBlock Text="Od datuma:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                    <DatePicker x:Name="DpDnevnikOd" Height="34" SelectedDateChanged="DpDnevnikFilter_SelectedDateChanged"/>
                                </StackPanel>

                                <StackPanel Orientation="Vertical" Margin="0,0,16,0" VerticalAlignment="Center">
                                    <TextBlock Text="Do datuma:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" FontWeight="SemiBold" Margin="0,0,0,3"/>
                                    <DatePicker x:Name="DpDnevnikDo" Height="34" SelectedDateChanged="DpDnevnikFilter_SelectedDateChanged"/>
                                </StackPanel>

                                <Button Content="🔄 Osveži Dnevnik" Style="{StaticResource ActionButtonStyle}" Height="34" Padding="14,0" VerticalAlignment="Bottom" ToolTip="Učitaj podatke dnevnika za izabrani period" Click="BtnOsveziDnevnik_Click"/>
                            </WrapPanel>
                        </DockPanel>
                    </Border>

                    <!-- SUMMARY KPI CARDS -->
                    <Grid Grid.Row="1" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="14" Margin="0,0,6,0" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="PRENETI SALDO (Početno)" FontSize="10" FontWeight="SemiBold" Foreground="#64748B"/>
                                <TextBlock x:Name="TxtPocetnoStanje" Text="0,00 RSD" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="14" Margin="6,0,6,0" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="UKUPNO UPLAĆENO (+)" FontSize="10" FontWeight="SemiBold" Foreground="#059669"/>
                                <TextBlock x:Name="TxtUkupnoUplata" Text="0,00 RSD" FontSize="16" FontWeight="Bold" Foreground="#059669" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="14" Margin="6,0,6,0" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="UKUPNO ISPLAĆENO (-)" FontSize="10" FontWeight="SemiBold" Foreground="#D97706"/>
                                <TextBlock x:Name="TxtUkupnoIsplata" Text="0,00 RSD" FontSize="16" FontWeight="Bold" Foreground="#D97706" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="3" Background="White" CornerRadius="8" Padding="14" Margin="6,0,0,0" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="NOVI SALDO BLAGAJNE (=)" FontSize="10" FontWeight="SemiBold" Foreground="#4F46E5"/>
                                <TextBlock x:Name="TxtKrajnjeStanje" Text="0,00 RSD" FontSize="16" FontWeight="Bold" Foreground="#4F46E5" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgDnevnik" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Broj naloga" Binding="{Binding BrojNaloga}" Width="130" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                                <DataGridTextColumn Header="Uplatilac / Isplatilac" Binding="{Binding UplatilacIsplatilac}" Width="180"/>
                                <DataGridTextColumn Header="Svrha" Binding="{Binding Svrha}" Width="*"/>
                                <DataGridTextColumn Header="Konto" Binding="{Binding BrojKontaProtu}" Width="80"/>
                                <DataGridTextColumn Header="Uplata (+)" Binding="{Binding Uplata, StringFormat=\{0:N2\}}" Width="120" Foreground="#059669" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Isplata (-)" Binding="{Binding Isplata, StringFormat=\{0:N2\}}" Width="120" Foreground="#D97706" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Saldo" Binding="{Binding Saldo, StringFormat=\{0:N2\}}" Width="130" FontWeight="Bold"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
