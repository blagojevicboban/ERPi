<Page x:Class="ERPiApp.Views.Sredstva.Dashboard.SredstvaDashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="Radna tabla"
      Background="{StaticResource SurfaceBrush}">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../SredstvaStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- KPI Kartice -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Ukupan broj sredstava -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Ukupno sredstava" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding UkupanBrojSredstava}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource SredstvaPrimaryLightBrush}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Ukupna nabavna vrednost -->
            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Ukupna nabavna vrednost" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding UkupnaNabavnaVrednost, StringFormat=N2}" FontSize="24" FontWeight="Bold" Foreground="#4A6572" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Ukupna sadasnja vrednost -->
            <Border Grid.Column="4" Style="{StaticResource Card}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Ukupna sadašnja vrednost" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding UkupnaSadasnjaVrednost, StringFormat=N2}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Grafikoni -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.8*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Top najvrednijih (horizontalni bar) -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Top 5 najvrednijih aktivnih sredstava" FontWeight="SemiBold" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                    <lvc:CartesianChart Grid.Row="1" Series="{Binding TopSredstvaSeries}" XAxes="{Binding TopSredstvaXAxes}" YAxes="{Binding TopSredstvaYAxes}" />
                </Grid>
            </Border>

            <!-- Desna kolona: Status sredstava i Vrednost po kontima, jedno ispod drugog -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Status sredstava (Donut) -->
                <Border Grid.Row="0" Style="{StaticResource Card}" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Status sredstava" FontWeight="SemiBold" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                        <lvc:PieChart Grid.Row="1" Series="{Binding StatusSeries}" LegendPosition="Right" />
                    </Grid>
                </Border>

                <!-- Vrednost po kontima (Pie) -->
                <Border Grid.Row="2" Style="{StaticResource Card}" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Sadašnja vrednost po kontima (Top 10)" FontWeight="SemiBold" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                        <lvc:PieChart Grid.Row="1" Series="{Binding KontoSeries}" LegendPosition="Right" />
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>
