<Window x:Class="ERPiApp.Views.Zarade.Podesavanja.ZaradeUvozProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Uvoz u toku"
        Height="420" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource BackgroundBrush}">

    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock x:Name="TxtNaslov" Text="⚡ Uvoz u toku" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                <TextBlock Text="Molimo sačekajte, uvoz podataka može potrajati nekoliko minuta..." FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Progress bar (indeterminate — pojedinačni koraci uvoza ne izveštavaju procenat) -->
            <ProgressBar x:Name="PbProgress" Grid.Row="1" Height="10" IsIndeterminate="True"
                         Foreground="{StaticResource PrimaryBrush}" Background="{StaticResource BorderBrush}"
                         BorderThickness="0" Margin="0,0,0,10"/>

            <!-- Trenutni status (poslednja log linija) -->
            <TextBlock x:Name="TxtStatus" Grid.Row="2" Text="Pokrećem uvoz..." FontWeight="SemiBold"
                       Foreground="{StaticResource TextBrush}" Margin="0,0,0,12" TextWrapping="Wrap"/>

            <!-- Log -->
            <Border Grid.Row="3" Background="#0F172A" CornerRadius="6" Padding="12">
                <ScrollViewer x:Name="LogScroll" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="TxtLog" Text="" FontFamily="Consolas" FontSize="12" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</Window>
