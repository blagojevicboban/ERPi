<UserControl x:Class="ERPiApp.Views.Magacin.MagaciniView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtPretraga" Width="220" Height="34" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretraga_TextChanged"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnNoviMagacin" Content="➕" Style="{StaticResource IconButtonStyle}" ToolTip="Novi magacin" Click="BtnNoviMagacin_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnIzmeniMagacin" Content="✏️" Style="{StaticResource IconButtonStyle}" ToolTip="Izmeni magacin" Click="BtnIzmeniMagacin_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnObrisiMagacin" Content="🗑️" Style="{StaticResource IconButtonStyle}" ToolTip="Obriši magacin" Click="BtnObrisiMagacin_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- LISTA MAGACINA -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="🏭 Šifarnik magacina" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                <DataGrid x:Name="DgMagacini" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                          SelectionChanged="DgMagacini_SelectionChanged" MouseDoubleClick="DgMagacini_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Šifra" Binding="{Binding SifraMagacina}" Width="120"/>
                        <DataGridTextColumn Header="Naziv magacina" Binding="{Binding NazivMagacina}" Width="*"/>
                        <DataGridTextColumn Header="Vrsta" Binding="{Binding VrstaMagacina}" Width="150"/>
                        <DataGridTextColumn Header="Odgovorno lice" Binding="{Binding OdgovornoLice}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
