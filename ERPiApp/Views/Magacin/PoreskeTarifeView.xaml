<UserControl x:Class="ERPiApp.Views.Magacin.PoreskeTarifeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtPretraga" Style="{StaticResource SearchTextBoxStyle}" Width="220" Height="34" VerticalContentAlignment="Center" TextChanged="TxtPretraga_TextChanged"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnNovaTarifa" Content="➕ Nova tarifa" Style="{StaticResource PrimaryButtonStyle}" ToolTip="Nova poreska tarifa" Click="BtnNovaTarifa_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnIzmeniTarifu" Content="✏️ Izmeni" Style="{StaticResource PrimaryButtonStyle}" Background="#D97706" ToolTip="Izmeni tarifu" Click="BtnIzmeniTarifu_Click" IsEnabled="False" Margin="0,0,8,0"/>
                    <Button x:Name="BtnObrisiTarifu" Content="🗑️ Obriši" Style="{StaticResource PrimaryButtonStyle}" Background="#DC2626" ToolTip="Obriši tarifu" Click="BtnObrisiTarifu_Click" IsEnabled="False" Margin="0,0,8,0"/>
                    <Button x:Name="BtnExportExcel" Content="X" FontWeight="Bold" FontSize="20" Style="{StaticResource PrimaryButtonStyle}" Background="#059669" Width="36" Padding="0" ToolTip="Izvoz poreskih tarifa u Excel" Click="BtnExportExcel_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- LISTA TARIFA -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="🏛️ Poreske tarife" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                <DataGrid x:Name="DgTarife" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                          SelectionChanged="DgTarife_SelectionChanged" MouseDoubleClick="DgTarife_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tar. broj" Binding="{Binding TarifniBroj}" Width="100"/>
                        <DataGridTextColumn Header="Porez %" Binding="{Binding PorezProcenat, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        <DataGridTextColumn Header="Poseban porez %" Binding="{Binding PosebanPorezProcenat, StringFormat=\{0:N2\}}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        <DataGridTextColumn Header="Porez u ceni" Binding="{Binding PorezUCeni}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
