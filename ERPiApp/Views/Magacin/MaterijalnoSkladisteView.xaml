<UserControl x:Class="ERPiApp.Views.Magacin.MaterijalnoSkladisteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid>
        <TabControl x:Name="TabMaterijalno" Background="Transparent" BorderThickness="0">

            <!-- ===================== ŠIFRARNIK MATERIJALA ===================== -->
            <TabItem x:Name="TabSifrarnikMaterijala" Header="🧱 Šifrarnik materijala">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel LastChildFill="True">
                            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="🔍 Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                                <TextBox x:Name="TxtPretragaSifrarnikMaterijala" Width="280" Height="34" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretragaSifrarnikMaterijala_TextChanged"/>
                            </StackPanel>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="BtnNoviMaterijal" Content="➕" Style="{StaticResource IconButtonStyle}" ToolTip="Novi materijal (F2)" Margin="0,0,8,0" Click="BtnNoviMaterijal_Click"/>
                                <Button x:Name="BtnIzmeniMaterijal" Content="✏️" Style="{StaticResource IconButtonStyle}" ToolTip="Izmeni izabrani materijal" Margin="0,0,8,0" Click="BtnIzmeniMaterijal_Click"/>
                                <Button x:Name="BtnBrisiMaterijal" Content="🗑️" Style="{StaticResource IconButtonStyle}" ToolTip="Obriši izabrani materijal" Margin="0,0,8,0" Click="BtnBrisiMaterijal_Click"/>
                                <Button x:Name="BtnExportExcelMaterijali" Content="X" FontWeight="Bold" FontSize="20" Width="36" Height="34" Background="#059669" Foreground="White" ToolTip="Izvoz šifrarnika materijala u Excel" Click="BtnExportExcelMaterijali_Click"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgSifrarnikMaterijala" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" MouseDoubleClick="DgSifrarnikMaterijala_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Šifra" Binding="{Binding SifraArtikla}" Width="90"/>
                                <DataGridTextColumn Header="Naziv materijala" Binding="{Binding Naziv}" Width="2*"/>
                                <DataGridTextColumn Header="J.M." Binding="{Binding JedinicaMere}" Width="60"/>
                                <DataGridTextColumn Header="Pakovanje" Binding="{Binding Pakovanje}" Width="150"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- ===================== ULAZI ===================== -->
            <TabItem x:Name="TabUlazi" Header="📥 Ulazi">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="1.2*"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel LastChildFill="True">
                            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="🔍 Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                                <TextBox x:Name="TxtPretragaUlaz" Width="200" Height="34" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretragaUlaz_TextChanged" Margin="0,0,16,0"/>
                                <RadioButton x:Name="RbSviUlazi" Content="Svi" GroupName="UlazStatusFilter" VerticalAlignment="Center" Margin="0,0,8,0" Checked="Filter_Ulazi_Changed"/>
                                <RadioButton x:Name="RbProknjizeniUlazi" Content="Proknjiženi" GroupName="UlazStatusFilter" VerticalAlignment="Center" Margin="0,0,8,0" Checked="Filter_Ulazi_Changed"/>
                                <RadioButton x:Name="RbNeproknjizeniUlazi" Content="Neproknjiženi" GroupName="UlazStatusFilter" VerticalAlignment="Center" Checked="Filter_Ulazi_Changed"/>
                            </StackPanel>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="BtnNoviUlaz" Content="➕" Style="{StaticResource IconButtonStyle}" ToolTip="Novi ulaz materijala (F2)" Margin="0,0,8,0" Click="BtnNoviUlaz_Click"/>
                                <Button x:Name="BtnIzmeniUlaz" Content="✏️" Style="{StaticResource IconButtonStyle}" ToolTip="Izmeni izabrani nalog ulaza" Margin="0,0,8,0" Click="BtnIzmeniUlaz_Click"/>
                                <Button x:Name="BtnKnjiziUlaz" Content="✅" Style="{StaticResource IconButtonStyle}" ToolTip="Proknjiži izabrani nalog ulaza" Margin="0,0,8,0" Click="BtnKnjiziUlaz_Click"/>
                                <Button x:Name="BtnExportExcelUlazi" Content="X" FontWeight="Bold" FontSize="20" Width="36" Height="34" Background="#059669" Foreground="White" ToolTip="Izvoz ulaza u Excel" Click="BtnExportExcelUlazi_Click"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgUlazi" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" SelectionChanged="DgUlazi_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Br. naloga" Binding="{Binding BrojNaloga}" Width="100"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                                <DataGridTextColumn Header="Magacin" Binding="{Binding Magacin.NazivMagacina}" Width="160"/>
                                <DataGridTextColumn Header="Br. računa" Binding="{Binding BrojRacuna}" Width="120"/>
                                <DataGridTextColumn Header="Proknjiženo" Binding="{Binding IsKnjizen}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="#E2E8F0"/>

                    <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="16" Margin="0,16,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgUlazStavke" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="R.br." Binding="{Binding RedniBroj}" Width="60"/>
                                <DataGridTextColumn Header="Materijal" Binding="{Binding Materijal.SifraArtikla}" Width="100"/>
                                <DataGridTextColumn Header="Naziv materijala" Binding="{Binding Materijal.Naziv}" Width="*"/>
                                <DataGridTextColumn Header="Količina" Binding="{Binding Kolicina, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="Cena" Binding="{Binding Cena, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="Iznos" Binding="{Binding Iznos, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- ===================== TREBOVANJA ===================== -->
            <TabItem x:Name="TabTrebovanja" Header="📤 Trebovanja">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="1.2*"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel LastChildFill="True">
                            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="🔍 Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                                <TextBox x:Name="TxtPretragaTrebovanje" Width="200" Height="34" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretragaTrebovanje_TextChanged" Margin="0,0,16,0"/>
                                <RadioButton x:Name="RbSviTrebovanja" Content="Svi" GroupName="TrebovanjeStatusFilter" VerticalAlignment="Center" Margin="0,0,8,0" Checked="Filter_Trebovanja_Changed"/>
                                <RadioButton x:Name="RbProknjizeniTrebovanja" Content="Proknjiženi" GroupName="TrebovanjeStatusFilter" VerticalAlignment="Center" Margin="0,0,8,0" Checked="Filter_Trebovanja_Changed"/>
                                <RadioButton x:Name="RbNeproknjizeniTrebovanja" Content="Neproknjiženi" GroupName="TrebovanjeStatusFilter" VerticalAlignment="Center" Checked="Filter_Trebovanja_Changed"/>
                            </StackPanel>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="BtnNovoTrebovanje" Content="➕" Style="{StaticResource IconButtonStyle}" ToolTip="Novo trebovanje (F2)" Margin="0,0,8,0" Click="BtnNovoTrebovanje_Click"/>
                                <Button x:Name="BtnIzmeniTrebovanje" Content="✏️" Style="{StaticResource IconButtonStyle}" ToolTip="Izmeni izabrano trebovanje" Margin="0,0,8,0" Click="BtnIzmeniTrebovanje_Click"/>
                                <Button x:Name="BtnKnjiziTrebovanje" Content="✅" Style="{StaticResource IconButtonStyle}" ToolTip="Proknjiži izabrano trebovanje" Margin="0,0,8,0" Click="BtnKnjiziTrebovanje_Click"/>
                                <Button x:Name="BtnExportExcelTrebovanja" Content="X" FontWeight="Bold" FontSize="20" Width="36" Height="34" Background="#059669" Foreground="White" ToolTip="Izvoz trebovanja u Excel" Click="BtnExportExcelTrebovanja_Click"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgTrebovanja" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" SelectionChanged="DgTrebovanja_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Br. naloga" Binding="{Binding BrojNaloga}" Width="100"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                                <DataGridTextColumn Header="Magacin" Binding="{Binding Magacin.NazivMagacina}" Width="160"/>
                                <DataGridTextColumn Header="Proknjiženo" Binding="{Binding IsKnjizen}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="#E2E8F0"/>

                    <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="16" Margin="0,16,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgTrebovanjeStavke" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="R.br." Binding="{Binding RedniBroj}" Width="60"/>
                                <DataGridTextColumn Header="Materijal" Binding="{Binding Materijal.SifraArtikla}" Width="100"/>
                                <DataGridTextColumn Header="Naziv materijala" Binding="{Binding Materijal.Naziv}" Width="*"/>
                                <DataGridTextColumn Header="Količina" Binding="{Binding Kolicina, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                <DataGridTextColumn Header="Konto troška" Binding="{Binding KontoTroska}" Width="110"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- ===================== PRIMOPREDAJE (M4) ===================== -->
            <TabItem x:Name="TabPrimopredaje" Header="🔄 Primopredaje">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="1.2*"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel LastChildFill="True">
                            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="🔍 Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                                <TextBox x:Name="TxtPretragaPrimopredaja" Width="200" Height="34" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretragaPrimopredaja_TextChanged" Margin="0,0,16,0"/>
                                <RadioButton x:Name="RbSviPrimopredaje" Content="Svi" GroupName="PrimopredajaStatusFilter" VerticalAlignment="Center" Margin="0,0,8,0" Checked="Filter_Primopredaje_Changed"/>
                                <RadioButton x:Name="RbProknjizeniPrimopredaje" Content="Proknjiženi" GroupName="PrimopredajaStatusFilter" VerticalAlignment="Center" Margin="0,0,8,0" Checked="Filter_Primopredaje_Changed"/>
                                <RadioButton x:Name="RbNeproknjizeniPrimopredaje" Content="Neproknjiženi" GroupName="PrimopredajaStatusFilter" VerticalAlignment="Center" Checked="Filter_Primopredaje_Changed"/>
                            </StackPanel>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="BtnNovaPrimopredaja" Content="➕" Style="{StaticResource IconButtonStyle}" ToolTip="Nova primopredaja (F2)" Margin="0,0,8,0" Click="BtnNovaPrimopredaja_Click"/>
                                <Button x:Name="BtnIzmeniPrimopredaju" Content="✏️" Style="{StaticResource IconButtonStyle}" ToolTip="Izmeni izabranu primopredaju" Margin="0,0,8,0" Click="BtnIzmeniPrimopredaju_Click"/>
                                <Button x:Name="BtnKnjiziPrimopredaju" Content="✅" Style="{StaticResource IconButtonStyle}" ToolTip="Proknjiži izabranu primopredaju" Margin="0,0,8,0" Click="BtnKnjiziPrimopredaju_Click"/>
                                <Button x:Name="BtnExportExcelPrimopredaje" Content="X" FontWeight="Bold" FontSize="20" Width="36" Height="34" Background="#059669" Foreground="White" ToolTip="Izvoz primopredaja u Excel" Click="BtnExportExcelPrimopredaje_Click"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgPrimopredaje" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" SelectionChanged="DgPrimopredaje_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Br. naloga" Binding="{Binding BrojNaloga}" Width="100"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                                <DataGridTextColumn Header="Daje (Izlaz)" Binding="{Binding MagacinDaje.NazivMagacina}" Width="150"/>
                                <DataGridTextColumn Header="Prima (Ulaz)" Binding="{Binding MagacinPrima.NazivMagacina}" Width="150"/>
                                <DataGridTextColumn Header="Proknjiženo" Binding="{Binding IsKnjizen}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="#E2E8F0"/>

                    <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="16" Margin="0,16,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DataGrid x:Name="DgPrimopredajaStavke" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="R.br." Binding="{Binding RedniBroj}" Width="60"/>
                                <DataGridTextColumn Header="Materijal" Binding="{Binding Materijal.SifraArtikla}" Width="120"/>
                                <DataGridTextColumn Header="Naziv materijala" Binding="{Binding Materijal.Naziv}" Width="*"/>
                                <DataGridTextColumn Header="Količina" Binding="{Binding Kolicina, StringFormat=\{0:N2\}}" Width="130" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- ===================== KARTICE MATERIJALA ===================== -->
            <TabItem x:Name="TabKartice" Header="📇 Kartice materijala">
                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,16,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel>
                            <TextBlock Text="Magacin" FontSize="12" Foreground="{StaticResource TextMutedBrush}" DockPanel.Dock="Top" Margin="0,0,0,4"/>
                            <ComboBox x:Name="CmbMagacin" DockPanel.Dock="Top" Height="34" Margin="0,0,0,12"
                                      DisplayMemberPath="Prikaz" SelectionChanged="CmbMagacin_SelectionChanged"/>

                            <TextBlock Text="🧱 Materijali" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                            <TextBox x:Name="TxtPretragaArtikla" DockPanel.Dock="Top" Height="34" VerticalContentAlignment="Center" Padding="8,0" Margin="0,0,0,8" ToolTip="Pretraga po šifri ili nazivu materijala" TextChanged="TxtPretragaArtikla_TextChanged"/>
                            <CheckBox x:Name="ChkSamoSaKarticom" Content="Samo materijali sa karticom" DockPanel.Dock="Top" Margin="0,0,0,12" Checked="ChkSamoSaKarticom_Changed" Unchecked="ChkSamoSaKarticom_Changed"/>
                            <DataGrid x:Name="LstArtikli" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" SelectionChanged="LstArtikli_SelectionChanged">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Šifra" Binding="{Binding SifraArtikla}" Width="90"/>
                                    <DataGridTextColumn Header="Naziv" Binding="{Binding Naziv}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </DockPanel>
                    </Border>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <DockPanel LastChildFill="True">
                                <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
                                    <TextBlock x:Name="TxtNaslovArtikla" Text="Izaberite magacin i materijal sa leve strane" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock x:Name="TxtStanjeArtikla" Text=" " FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button x:Name="BtnProveraKartica" Content="⚠️" Style="{StaticResource IconButtonStyle}" ToolTip="Provera negativnih stanja u materijalnim karticama" Margin="0,0,8,0" Click="BtnProveraKartica_Click"/>
                                    <Button x:Name="BtnExportExcelKartica" Content="X" FontWeight="Bold" FontSize="20" Width="36" Height="34" Background="#059669" Foreground="White" ToolTip="Izvoz kartice materijala u Excel" Click="BtnExportExcelKartica_Click"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>

                        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <DataGrid x:Name="DgKarticaMaterijala" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Datum" Binding="{Binding DatumPromene, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                                        <DataGridTextColumn Header="Opis" Binding="{Binding OpisPromene}" Width="160"/>
                                        <DataGridTextColumn Header="Ulaz" Binding="{Binding Ulaz, StringFormat=\{0:N2\}}" Width="90" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                        <DataGridTextColumn Header="Izlaz" Binding="{Binding Izlaz, StringFormat=\{0:N2\}}" Width="90" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                        <DataGridTextColumn Header="Stanje" Binding="{Binding Stanje, StringFormat=\{0:N2\}}" Width="90" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                        <DataGridTextColumn Header="Cena" Binding="{Binding Cena, StringFormat=\{0:N2\}}" Width="100" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                        <DataGridTextColumn Header="Duguje" Binding="{Binding Duguje, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                        <DataGridTextColumn Header="Potražuje" Binding="{Binding Potrazuje, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                        <DataGridTextColumn Header="Saldo" Binding="{Binding Saldo, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                        <DataGridTextColumn Header="Pros. cena" Binding="{Binding CenaIzlaz, StringFormat=\{0:N2\}}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Border Grid.Row="1" Background="#EFF6FF" CornerRadius="6" Padding="12,10" Margin="0,8,0,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="90"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="1" Text="UKUPNO:" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="TxtSumaUlazMaterijal" Grid.Column="2" Text="0,00" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="TxtSumaIzlazMaterijal" Grid.Column="3" Text="0,00" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="TxtSumaDugujeMaterijal" Grid.Column="6" Text="0,00" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="TxtSumaPotrazujeMaterijal" Grid.Column="7" Text="0,00" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="TxtSumaSaldoMaterijal" Grid.Column="8" Text="0,00" FontWeight="Bold" FontSize="15" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#2563EB"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- ===================== BRUTO BILANS MATERIJALNOG KNJIGOVODSTVA ===================== -->
            <TabItem x:Name="TabBrutoBilansMaterijala" Header="📊 Bruto bilans materijalnog knjigovodstva">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel LastChildFill="True">
                            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Magacin:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                <ComboBox x:Name="CmbMagacinBrutoMat" Width="200" Height="34" DisplayMemberPath="Prikaz" SelectedValuePath="MagacinId" SelectionChanged="CmbMagacinBrutoMat_SelectionChanged" Margin="0,0,16,0"/>

                                <TextBlock Text="Do datuma:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                <DatePicker x:Name="DpDoDatumaBrutoMat" Width="130" Height="34" SelectedDateChanged="DpDoDatumaBrutoMat_SelectedDateChanged" Margin="0,0,16,0"/>

                                <TextBlock Text="🔍 Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                <TextBox x:Name="TxtPretragaBrutoMat" Width="180" Height="34" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretragaBrutoMat_TextChanged"/>
                            </StackPanel>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="BtnOsveziBrutoMat" Content="🔄" Style="{StaticResource IconButtonStyle}" ToolTip="Osveži bilans" Margin="0,0,8,0" Click="BtnOsveziBrutoMat_Click"/>
                                <Button x:Name="BtnExportExcelBrutoMat" Content="X" FontWeight="Bold" FontSize="20" Width="36" Height="34" Background="#059669" Foreground="White" ToolTip="Izvoz bruto bilansa materijala u Excel" Click="BtnExportExcelBrutoMat_Click"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <DockPanel>
                            <TextBlock Text="📊 BRUTO BILANS MATERIJALNOG KNJIGOVODSTVA" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                            <DataGrid x:Name="DgBrutoBilansMat" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Magacin" Binding="{Binding SifraMagacina}" Width="80"/>
                                    <DataGridTextColumn Header="Šifra" Binding="{Binding SifraArtikla}" Width="100"/>
                                    <DataGridTextColumn Header="Naziv materijala" Binding="{Binding NazivArtikla}" Width="*"/>
                                    <DataGridTextColumn Header="J.M." Binding="{Binding JedinicaMere}" Width="50"/>
                                    <DataGridTextColumn Header="Cena" Binding="{Binding Cena, StringFormat=\{0:N2\}}" Width="90" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                    <DataGridTextColumn Header="Ulaz kol." Binding="{Binding UlazKolicina, StringFormat=\{0:N2\}}" Width="90" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                    <DataGridTextColumn Header="Duguje (Ulaz RSD)" Binding="{Binding UlazVrednost, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                    <DataGridTextColumn Header="Izlaz kol." Binding="{Binding IzlazKolicina, StringFormat=\{0:N2\}}" Width="90" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                    <DataGridTextColumn Header="Potražuje (Izlaz RSD)" Binding="{Binding IzlazVrednost, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                    <DataGridTextColumn Header="Stanje kol." Binding="{Binding SaldoKolicinski, StringFormat=\{0:N2\}}" Width="90" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                    <DataGridTextColumn Header="Saldo zaliha (RSD)" Binding="{Binding SaldoVrednosni, StringFormat=\{0:N2\}}" Width="130" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </DockPanel>
                    </Border>

                    <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16,10" Margin="0,12,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock x:Name="TxtUkupnoDugujeBrutoMat" Text="Ukupno Duguje: 0,00 RSD" FontSize="13" FontWeight="Bold" Foreground="#059669" Margin="0,0,24,0"/>
                            <TextBlock x:Name="TxtUkupnoPotrazujeBrutoMat" Text="Ukupno Potražuje: 0,00 RSD" FontSize="13" FontWeight="Bold" Foreground="#DC2626" Margin="0,0,24,0"/>
                            <TextBlock x:Name="TxtUkupnoSaldoBrutoMat" Text="Saldo Zaliha: 0,00 RSD" FontSize="14" FontWeight="Bold" Foreground="#2563EB"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
