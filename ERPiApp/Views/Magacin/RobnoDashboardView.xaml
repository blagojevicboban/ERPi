<UserControl x:Class="ERPiApp.Views.Magacin.RobnoDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="950" d:DesignWidth="1100">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Quick Actions -->
                <RowDefinition Height="Auto"/> <!-- Vrednost zaliha KPIs -->
                <RowDefinition Height="Auto"/> <!-- Poslednje kalkulacije i nivelacije -->
                <RowDefinition Height="Auto"/> <!-- Top artikli -->
            </Grid.RowDefinitions>

            <!-- ⚡ BRZE AKCIJE -->
            <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="16,14" Margin="0,0,0,20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                        <TextBlock Text="⚡" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="BRZE AKCIJE:" FontSize="13" FontWeight="Bold" Foreground="#334155" VerticalAlignment="Center"/>
                    </StackPanel>

                    <WrapPanel Grid.Column="1" VerticalAlignment="Center">
                        <Button x:Name="BtnNovaKalkulacijaVp" Content="🛒 Nova kalkulacija (VP)" Style="{StaticResource ActionButtonStyle}" Height="36" Padding="14,0" Margin="0,0,10,6" Click="BtnNovaKalkulacijaVp_Click"/>
                        <Button x:Name="BtnNovaNivelacija" Content="🏷️ Nova nivelacija" Style="{StaticResource ActionButtonStyle}" Height="36" Padding="14,0" Margin="0,0,10,6" Background="#0D9488" Click="BtnNovaNivelacija_Click"/>
                        <Button x:Name="BtnNovaOtpremnica" Content="📜 Nova otpremnica" Style="{StaticResource ActionButtonStyle}" Height="36" Padding="14,0" Margin="0,0,10,6" Background="#475569" Click="BtnNovaOtpremnica_Click"/>
                        <Button x:Name="BtnNovaPrimopredaja" Content="🔄 Nova primopredaja" Style="{StaticResource ActionButtonStyle}" Height="36" Padding="14,0" Margin="0,0,0,6" Background="#2563EB" Click="BtnNovaPrimopredaja_Click"/>
                    </WrapPanel>
                </Grid>
            </Border>

            <!-- 💰 VREDNOST ZALIHA -->
            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                <TextBlock Text="VREDNOST ZALIHA ROBE" FontSize="12" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,10"/>
                <UniformGrid Columns="3">
                    <Border Background="White" CornerRadius="12" Padding="18,16" Margin="0,0,8,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="VELEPRODAJA (VP)" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtVrednostZalihaVp" Text="0 RSD" FontSize="22" FontWeight="Bold" Foreground="#2563EB" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Background="White" CornerRadius="12" Padding="18,16" Margin="8,0,8,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="MALOPRODAJA (MP)" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtVrednostZalihaMp" Text="0 RSD" FontSize="22" FontWeight="Bold" Foreground="#7C3AED" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Background="White" CornerRadius="12" Padding="18,16" Margin="8,0,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="UKUPNA VREDNOST ZALIHA" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtVrednostZalihaUkupno" Text="0 RSD" FontSize="22" FontWeight="Bold" Foreground="#059669" Margin="0,6,0,0"/>
                            <TextBlock x:Name="TxtBrojArtikala" Text="0 artikala na zalihi" FontSize="11" Foreground="#10B981" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>
            </StackPanel>

            <!-- 📋 POSLEDNJE KALKULACIJE I NIVELACIJE -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="🛒 Poslednje kalkulacije" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>
                        <DataGrid x:Name="DgPoslednjeKalkulacije" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" RowHeight="32">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Tip" Binding="{Binding Tip}" Width="45"/>
                                <DataGridTextColumn Header="Broj" Binding="{Binding BrojKalkulacije}" Width="60"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="*" MinWidth="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>

                <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="🏷️ Poslednje nivelacije" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>
                        <DataGrid x:Name="DgPoslednjeNivelacije" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" RowHeight="32">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Broj" Binding="{Binding BrojNivelacije}" Width="60"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding DatumNivelacije, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                                <DataGridTextColumn Header="Magacin" Binding="{Binding NazivMagacina}" Width="*" MinWidth="100"/>
                                <DataGridTextColumn Header="Razlika (RSD)" Binding="{Binding UkupnoRazlika, StringFormat=\{0:N2\}}" Width="100" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- 📦 TOP ARTIKLI -->
            <Border Grid.Row="3" Background="White" CornerRadius="12" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="📦 Top 10 artikala po prometu / vrednosti zaliha" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>
                    <DataGrid x:Name="DgTopArtikli" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" RowHeight="32">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Šifra" Binding="{Binding SifraArtikla}" Width="90"/>
                            <DataGridTextColumn Header="Naziv artikla" Binding="{Binding NazivArtikla}" Width="*" MinWidth="200"/>
                            <DataGridTextColumn Header="Vrednost zaliha (RSD)" Binding="{Binding VrednostZaliha, StringFormat=N2}" Width="160" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            <DataGridTextColumn Header="Promet (RSD)" Binding="{Binding Promet, StringFormat=N2}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
