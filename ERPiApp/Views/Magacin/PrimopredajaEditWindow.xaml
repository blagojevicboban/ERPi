<Window x:Class="ERPiApp.Views.Magacin.PrimopredajaEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nova primopredaja (interni prenos)" Height="600" Width="920"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}" KeyDown="Window_KeyDown">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Broj naloga" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                    <TextBox x:Name="TxtBrojNaloga" Width="90" Height="32" VerticalContentAlignment="Center" Padding="6,0"/>
                </StackPanel>
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Datum" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                    <DatePicker x:Name="DpDatum" Width="130" Height="32"/>
                </StackPanel>
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Magacin koji daje *" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                    <ComboBox x:Name="CmbMagacinDaje" Width="200" Height="32" DisplayMemberPath="Prikaz"/>
                </StackPanel>
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Magacin koji prima *" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                    <ComboBox x:Name="CmbMagacinPrima" Width="200" Height="32" DisplayMemberPath="Prikaz"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="PDV stopa (%)" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                    <TextBox x:Name="TxtStopaPdv" Width="80" Height="32" Text="20" VerticalContentAlignment="Center" Padding="6,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="📦 Stavke prenosa" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button x:Name="BtnDodajStavku" Content="➕" Style="{StaticResource IconButtonStyle}" ToolTip="Dodaj stavku" Click="BtnDodajStavku_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnObrisiStavku" Content="🗑️" Style="{StaticResource IconButtonStyle}" ToolTip="Obriši izabranu stavku" Click="BtnObrisiStavku_Click"/>
                </StackPanel>

                <DataGrid x:Name="DgStavke" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="R.br." Binding="{Binding RedniBroj}" Width="50" IsReadOnly="True"/>
                        <DataGridComboBoxColumn x:Name="ColMaterijal" Header="Materijal *"
                                                  SelectedValueBinding="{Binding MaterijalId}"
                                                  SelectedValuePath="MaterijalId"
                                                  DisplayMemberPath="Prikaz"
                                                  Width="320"/>
                        <DataGridTextColumn Header="Količina" Binding="{Binding Kolicina, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}" EditingElementStyle="{StaticResource NumericColumnEditingStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="BtnOtkazi" Content="Otkaži" Style="{StaticResource ActionButtonStyle}" Background="#64748B" Click="BtnOtkazi_Click" Margin="0,0,10,0"/>
            <Button x:Name="BtnSnimi" Content="💾 Snimi primopredaju" Style="{StaticResource ActionButtonStyle}" Background="#10B981" Click="BtnSnimi_Click"/>
        </StackPanel>
    </Grid>
</Window>
