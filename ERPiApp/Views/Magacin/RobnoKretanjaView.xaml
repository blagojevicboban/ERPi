<UserControl x:Class="ERPiApp.Views.Magacin.RobnoKretanjaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="180"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button x:Name="BtnNovi" Content="➕" Style="{StaticResource IconButtonStyle}" ToolTip="Novi dokument" Click="BtnNovi_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnIzmeni" Content="✏️" Style="{StaticResource IconButtonStyle}" ToolTip="Izmeni izabrani dokument" Click="BtnIzmeni_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnObrisi" Content="🗑️" Style="{StaticResource IconButtonStyle}" ToolTip="Obriši izabrani dokument" Click="BtnObrisi_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnKnjizi" Content="✅" Style="{StaticResource IconButtonStyle}" ToolTip="Proknjiži izabrani dokument" Click="BtnKnjizi_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnRasknjizi" Content="↩️" Style="{StaticResource IconButtonStyle}" ToolTip="Rasknjiži izabrani dokument" Click="BtnRasknjizi_Click" Margin="0,0,16,0"/>
                    <RadioButton x:Name="RbSvi" Content="Svi" GroupName="StatusFilterRK" VerticalAlignment="Center" Margin="0,0,8,0" Checked="Filter_Changed"/>
                    <RadioButton x:Name="RbProknjizeni" Content="Proknjiženi" GroupName="StatusFilterRK" VerticalAlignment="Center" Margin="0,0,8,0" Checked="Filter_Changed"/>
                    <RadioButton x:Name="RbNeproknjizeni" Content="Neproknjiženi" GroupName="StatusFilterRK" VerticalAlignment="Center" Checked="Filter_Changed"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <TextBlock Text="Pretraga:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource TextMutedBrush}"/>
                    <TextBox x:Name="TxtPretraga" Width="200" Height="34" VerticalContentAlignment="Center" Padding="6,0" TextChanged="TxtPretraga_TextChanged"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- GRID -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DataGrid x:Name="DgKretanja" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                      SelectionChanged="DgKretanja_SelectionChanged" MouseDoubleClick="DgKretanja_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Br. naloga" Binding="{Binding BrojNaloga}" Width="110"/>
                    <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                    <DataGridTextColumn Header="Iz magacina (daje)" Binding="{Binding MagacinDaje.NazivMagacina}" Width="1.5*"/>
                    <DataGridTextColumn Header="U magacin (prima)" Binding="{Binding MagacinPrima.NazivMagacina}" Width="1.5*"/>
                    <DataGridCheckBoxColumn Header="Proknjiženo" Binding="{Binding IsKnjizen}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="Transparent"/>

        <!-- STAVKE -->
        <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="📋 Stavke izabranog dokumenta" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,8"/>
                <DataGrid x:Name="DgStavke" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="R.br." Binding="{Binding RedniBroj}" Width="60"/>
                        <DataGridTextColumn Header="Šifra" Binding="{Binding Artikal.SifraArtikla}" Width="100"/>
                        <DataGridTextColumn Header="Naziv artikla" Binding="{Binding Artikal.Naziv}" Width="*"/>
                        <DataGridTextColumn Header="J.M." Binding="{Binding Artikal.JedinicaMere}" Width="60"/>
                        <DataGridTextColumn Header="Količina" Binding="{Binding Kolicina, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
