<UserControl x:Class="ERPiApp.Views.Magacin.MaterijalnoDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ERPiApp.Views.Magacin"
             mc:Ignorable="d"
             d:DesignHeight="950" d:DesignWidth="1100">
    <UserControl.Resources>
        <local:StatusUKnjizenjuVisibilityConverter x:Key="StatusUKnjizenjuVisibilityConverter"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Quick Actions -->
                <RowDefinition Height="Auto"/> <!-- Vrednost zaliha KPIs -->
                <RowDefinition Height="Auto"/> <!-- Poslednji ulazi i trebovanja -->
                <RowDefinition Height="Auto"/> <!-- Top materijali -->
            </Grid.RowDefinitions>

            <!-- ⚡ BRZE AKCIJE -->
            <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="16,14" Margin="0,0,0,20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                        <TextBlock Text="⚡" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="BRZE AKCIJE:" FontSize="13" FontWeight="Bold" Foreground="#334155" VerticalAlignment="Center"/>
                    </StackPanel>

                    <WrapPanel Grid.Column="1" VerticalAlignment="Center">
                        <Button x:Name="BtnNoviUlaz" Content="📥 Novi ulaz" Style="{StaticResource ActionButtonStyle}" Height="36" Padding="14,0" Margin="0,0,10,6" Click="BtnNoviUlaz_Click"/>
                        <Button x:Name="BtnNovoTrebovanje" Content="📤 Novo trebovanje" Style="{StaticResource ActionButtonStyle}" Height="36" Padding="14,0" Margin="0,0,10,6" Background="#7C3AED" Click="BtnNovoTrebovanje_Click"/>
                        <Button x:Name="BtnNovaPrimopredaja" Content="🔄 Nova primopredaja" Style="{StaticResource ActionButtonStyle}" Height="36" Padding="14,0" Margin="0,0,10,6" Background="#2563EB" Click="BtnNovaPrimopredaja_Click"/>
                    </WrapPanel>
                </Grid>
            </Border>

            <!-- 💰 VREDNOST ZALIHA -->
            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                <TextBlock Text="VREDNOST ZALIHA MATERIJALA" FontSize="12" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,10"/>
                <UniformGrid Columns="3">
                    <Border Background="White" CornerRadius="12" Padding="18,16" Margin="0,0,8,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="UKUPNA VREDNOST ZALIHA" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtVrednostZaliha" Text="0 RSD" FontSize="22" FontWeight="Bold" Foreground="#059669" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Background="White" CornerRadius="12" Padding="18,16" Margin="8,0,8,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="MATERIJALA NA ZALIHI" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtBrojMaterijala" Text="0" FontSize="22" FontWeight="Bold" Foreground="#2563EB" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Background="White" CornerRadius="12" Padding="18,16" Margin="8,0,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="NEGATIVNA STANJA" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtNegativnaStanja" Text="0" FontSize="22" FontWeight="Bold" Foreground="#DC2626" Margin="0,6,0,0"/>
                            <TextBlock Text="Materijali sa negativnom zalihom ili cenom" FontSize="11" Foreground="#94A3B8" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>
            </StackPanel>

            <!-- 📋 POSLEDNJI ULAZI I TREBOVANJA -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="📥 Poslednji ulazi" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>
                        <DataGrid x:Name="DgPoslednjiUlazi" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" RowHeight="32">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Broj" Binding="{Binding BrojNaloga}" Width="60"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                                <DataGridTextColumn Header="Magacin" Binding="{Binding SifraMagacina}" Width="*" MinWidth="80"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="90"/>
                                <DataGridTemplateColumn Width="130">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="Otvori" Padding="4,2" FontSize="11" Click="BtnOtvoriUlaz_Click" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Height="24" Margin="0,0,4,0"/>
                                                <Button Content="Knjiži" Padding="4,2" FontSize="11" Click="BtnKnjiziUlaz_Click" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Background="#10B981" Height="24"
                                                        Visibility="{Binding StatusText, Converter={StaticResource StatusUKnjizenjuVisibilityConverter}}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>

                <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="📤 Poslednja trebovanja" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>
                        <DataGrid x:Name="DgPoslednjaTrebovanja" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" RowHeight="32">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Broj" Binding="{Binding BrojNaloga}" Width="60"/>
                                <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}" Width="90"/>
                                <DataGridTextColumn Header="Magacin" Binding="{Binding SifraMagacina}" Width="*" MinWidth="80"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="90"/>
                                <DataGridTemplateColumn Width="130">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="Otvori" Padding="4,2" FontSize="11" Click="BtnOtvoriTrebovanje_Click" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Height="24" Margin="0,0,4,0"/>
                                                <Button Content="Knjiži" Padding="4,2" FontSize="11" Click="BtnKnjiziTrebovanje_Click" CommandParameter="{Binding}" Style="{StaticResource ActionButtonStyle}" Background="#10B981" Height="24"
                                                        Visibility="{Binding StatusText, Converter={StaticResource StatusUKnjizenjuVisibilityConverter}}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- 📦 TOP MATERIJALI -->
            <Border Grid.Row="3" Background="White" CornerRadius="12" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="📦 Top 10 materijala po prometu / vrednosti zaliha" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>
                    <DataGrid x:Name="DgTopMaterijali" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" RowHeight="32">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Šifra" Binding="{Binding SifraArtikla}" Width="90"/>
                            <DataGridTextColumn Header="Naziv materijala" Binding="{Binding NazivArtikla}" Width="*" MinWidth="200"/>
                            <DataGridTextColumn Header="Vrednost zaliha (RSD)" Binding="{Binding VrednostZaliha, StringFormat=N2}" Width="160" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            <DataGridTextColumn Header="Promet (RSD)" Binding="{Binding Promet, StringFormat=N2}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
