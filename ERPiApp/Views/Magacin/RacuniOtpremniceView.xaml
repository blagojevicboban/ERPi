<UserControl x:Class="ERPiApp.Views.Magacin.RacuniOtpremniceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button x:Name="BtnNoviRacun" Content="➕ Novi račun - otpremnica" Style="{StaticResource ActionButtonStyle}" Click="BtnNoviRacun_Click" Margin="0,0,10,0"/>
                    <Button x:Name="BtnIzmeni" Content="✏️ Izmeni" Style="{StaticResource ActionButtonStyle}" Background="#3B82F6" Click="BtnIzmeni_Click" Margin="0,0,10,0"/>
                    <Button x:Name="BtnKnjizi" Content="⚡ Knjiži" Style="{StaticResource ActionButtonStyle}" Background="#10B981" Click="BtnKnjizi_Click" Margin="0,0,10,0"/>
                    <Button x:Name="BtnRasknjizi" Content="↩️ Rasknjiži" Style="{StaticResource ActionButtonStyle}" Background="#F59E0B" Click="BtnRasknjizi_Click" Margin="0,0,10,0"/>
                    <Button x:Name="BtnPretvoriPredracun" Content="🔄 Pretvori u fakturu" Style="{StaticResource ActionButtonStyle}" Background="#7C3AED" ToolTip="Pretvara izabrani predračun u pravi račun-otpremnicu" Click="BtnPretvoriPredracun_Click" Margin="0,0,10,0"/>
                    <Button x:Name="BtnStampajPdf" Content="🖨️ PDF" Style="{StaticResource ActionButtonStyle}" Background="#4F46E5" Click="BtnStampajPdf_Click" Margin="0,0,10,0"/>
                    <Button x:Name="BtnExportExcel" Content="X" FontWeight="Bold" FontSize="20" Style="{StaticResource ActionButtonStyle}" Background="#059669" Width="36" Padding="0" ToolTip="Izvoz računa u Excel" Click="BtnExportExcel_Click" Margin="0,0,10,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <TextBlock Text="Pretraga:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource TextMutedBrush}"/>
                    <TextBox x:Name="TxtPretraga" Width="200" Height="34" VerticalContentAlignment="Center" Padding="6,0" TextChanged="TxtPretraga_TextChanged"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- GRID -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DataGrid x:Name="DgRacuni" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" MouseDoubleClick="DgRacuni_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Broj računa" Binding="{Binding BrojRacuna}" Width="100"/>
                    <DataGridTextColumn Header="Tip" Binding="{Binding TipDokumenta}" Width="100"/>
                    <DataGridTextColumn Header="Datum" Binding="{Binding DatumRacuna, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                    <DataGridTextColumn Header="Kupac" Binding="{Binding Partner.Naziv}" Width="2*"/>
                    <DataGridTextColumn Header="Magacin" Binding="{Binding Magacin.NazivMagacina}" Width="1.5*"/>
                    <DataGridTextColumn Header="Osnovica" Binding="{Binding UkupnoOsnovica, StringFormat=\{0:N2\}}" Width="120" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                    <DataGridTextColumn Header="PDV" Binding="{Binding UkupnoPdv, StringFormat=\{0:N2\}}" Width="110" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                    <DataGridTextColumn Header="Za uplatu" Binding="{Binding UkupnoZaUplatu, StringFormat=\{0:N2\}}" Width="130" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                    <DataGridCheckBoxColumn Header="Proknjižen" Binding="{Binding IsKnjizen}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
