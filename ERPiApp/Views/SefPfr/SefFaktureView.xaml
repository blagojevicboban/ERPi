<UserControl x:Class="ERPiApp.Views.SefPfr.SefFaktureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtPretraga" Width="220" Height="34" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretraga_TextChanged"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnUlazneFakture" Content="📥" Style="{StaticResource IconButtonStyle}" ToolTip="Preuzmi ulazne e-fakture dobavljača sa SEF-a" Click="BtnUlazneFakture_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnPosaljiNaSef" Content="🚀" Style="{StaticResource IconButtonStyle}" ToolTip="Pošalji izabrani račun na SEF (pravno lice — ima PIB)" Click="BtnPosaljiNaSef_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnFiskalizuj" Content="🧾" Style="{StaticResource IconButtonStyle}" ToolTip="Fiskalizuj preko PFR-a (fizičko lice / maloprodaja)" Click="BtnFiskalizuj_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnSacuvajUbl" Content="💾" Style="{StaticResource IconButtonStyle}" ToolTip="Sačuvaj UBL 2.1 XML fajl" Click="BtnSacuvajUbl_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnOsveziStatus" Content="🔄" Style="{StaticResource IconButtonStyle}" ToolTip="Osveži status sa SEF-a" Click="BtnOsveziStatus_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- LISTA RAČUNA -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="📄 Proknjiženi računi — SEF e-Fakture (UBL 2.1) i PFR fiskalizacija" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                <DataGrid x:Name="DgSefFakture" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                          SelectionChanged="DgSefFakture_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Broj računa" Binding="{Binding BrojRacuna}" Width="100"/>
                        <DataGridTextColumn Header="Datum" Binding="{Binding DatumRacuna, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                        <DataGridTextColumn Header="Partner / Kupac" Binding="{Binding Partner.Naziv}" Width="*"/>
                        <DataGridTextColumn Header="PIB" Binding="{Binding Partner.Pib}" Width="100"/>
                        <DataGridTextColumn Header="SEF status" Binding="{Binding SefStatus}" Width="110"/>
                        <DataGridTextColumn Header="Fiskalni broj" Binding="{Binding FiskalniBroj}" Width="140"/>
                        <DataGridTextColumn Header="Ukupno (RSD)" Binding="{Binding UkupnoZaUplatu, StringFormat=\{0:N2\}}" Width="130" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
