<UserControl x:Class="ERPiApp.Views.SefPfr.PfrRacuniView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Pretraga:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtPretraga" Width="220" Height="34" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretraga_TextChanged"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnNoviRacun" Content="➕" Style="{StaticResource IconButtonStyle}" ToolTip="Novi fiskalni račun" Click="BtnNoviRacun_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnFiskalizuj" Content="🧾" Style="{StaticResource IconButtonStyle}" ToolTip="Fiskalizuj izabrani račun na PFR" Click="BtnFiskalizuj_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- LISTA PFR RACUNA -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="🧾 e-Fiskalizacija (PFR fiskalni računi)" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                <DataGrid x:Name="DgPfrRacuni" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                          SelectionChanged="DgPfrRacuni_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Broj računa" Binding="{Binding BrojRacuna}" Width="140"/>
                        <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy HH:mm\}}" Width="130"/>
                        <DataGridTextColumn Header="Kupac / Partner" Binding="{Binding Partner.Naziv}" Width="*"/>
                        <DataGridTextColumn Header="Tip prometa" Binding="{Binding TipRacuna}" Width="130"/>
                        <DataGridTextColumn Header="PFR Broj" Binding="{Binding PfrBroj}" Width="160"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110"/>
                        <DataGridTextColumn Header="Iznos (RSD)" Binding="{Binding Iznos, StringFormat=\{0:N2\}}" Width="130" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
